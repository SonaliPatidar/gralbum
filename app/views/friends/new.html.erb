<p>Invite friend</p>
<%= form_tag user_friends_path(current_user.id) do %>
  <div class="form-group">
    <%= email_field_tag 'email'%>
  </div>
  <div class="form-group">
    <%= submit_tag 'Send Request' %>
  </div>
<% end %>